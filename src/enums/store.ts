export enum Filters {
    DEFAULT,
    COMPOSITION,
    ENSEMBLE,
    TRACK_LENGTH,
}

export enum trackSections {
    ID = 'id',
    COMPOSER = 'composer',
    COMPOSITION = 'composition',
    MOVEMENT = 'movement',
    ENSEMBLE = 'ensemble',
    SOURCE = 'source',
    TRANSCRIBER = 'transcriber',
    CATALOG_NAME = 'catalog_name',
    SECONDS = 'seconds',
}